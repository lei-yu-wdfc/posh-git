namespace Wonga.QA.Framework.Core
{
    public static class Constants
    {
         public static class Ca
         {
             public const int DefaultDaysUntilStartOfLoan = 1; // The Promise date in CA is always one day more than the loan term because loan payout can take a day
         }
    }
}